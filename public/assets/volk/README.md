# Volk brand assets

- `volk-sin-fondo.png` — the mark on a transparent background, used inline
  by `src/components/Logo.jsx` (Header, Footer, Hero) since the site is
  black throughout.
- `volk.png` — the mark on its black square. Not currently referenced;
  favicons come from `public/assets/favicon/` instead (see
  `index.html`/`favicon/site.webmanifest`).
